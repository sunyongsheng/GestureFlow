# Route B SwiftUI Settings Notes

## Route B Constraints

- Target runtime is macOS 14+
- Keep `GestureFlowApplication` as coordinator
- Keep AppKit status bar menu for this migration
- Replace only the settings window shell with SwiftUI scene management
