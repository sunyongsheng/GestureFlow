import CoreGraphics

enum SettingsWindowMetrics {
    /// Default content size for the settings window (fits gesture table + app list).
    static let defaultContentSize = CGSize(width: 1060, height: 660)
    static let minimumContentSize = CGSize(width: 1000, height: 620)
}
