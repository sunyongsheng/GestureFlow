import CoreGraphics

enum SettingsWindowMetrics {
    /// Default content size for the settings window (fits gesture table + app list).
    static let defaultContentSize = CGSize(width: 800, height: 600)
    static let minimumContentSize = CGSize(width: 720, height: 380)
}
